name: Deploy Website to InfinityFree

on:
  push:
    branches:
      - main  # Adjust to your deployment branch

jobs:
  deploy:
    name: FTP Deploy
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Deploy to InfinityFree
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: pkdev.infinityfreeapp.com
        username: if0_37757426
        password: eGIkJ992hv
        #local-dir: "public/" # Change this to your local build/output directory
        #server-dir: "/htdocs" # Ensure this matches InfinityFree's web root
        #log-level: debug  # This will provide detailed logs
